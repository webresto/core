{
  "key": "PASSWORD_POLICY",
  "type": "string",
  "name": "Password Policy",
  "description": "Defines the password requirement policy for user accounts. The options are: 'required' (password must be set), 'from_otp' (password is generated from a one-time password), or 'disabled' (no password is required).",
  "defaultValue": "from_otp",
  "jsonSchema": {
    "type": "string",
    "enum": ["required", "from_otp", "disabled"]
  },
  "isRequired": true
}
