{
  "key": "PROJECT_NAME",
  "type": "string",
  "name": "Project Name",
  "description": "Technical identifier of the project — used in metric labels, integration ids and as the default OTP sender name, not as the venue's display name. Lowercase latin letters, digits and hyphens only, at least 3 characters. Example: resto-acceptance",
  "jsonSchema": {
    "type": "string",
    "pattern": "^[a-z0-9\\-]+$",
    "minLength": 3
  },
  "isRequired": true
}
