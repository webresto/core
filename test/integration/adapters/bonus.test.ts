process.env.CAPTCHA_POW_DIFFICUTLY = "250000"
describe("Bonus adapter", function () {
  this.timeout(60000)

  before(function (done) {
    
  });

  it("get captcha POW task", async () => {

  });
});