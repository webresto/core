{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "key": {
      "type": "string",
      "description": "The key identifier"
    },
    "type": {
      "type": "string",
      "enum": ["string", "boolean", "json", "number"],
      "description": "The type"
    },
    "name": {
      "type": "string",
      "description": "The name"
    },
    "description": {
      "type": "string",
      "description": "The description of the description."
    },
    "defaultValue": {
      "type": ["string", "boolean", "object", "number", "array", "null"],
      "description": "The default value"
    },
    "value": {
      "type": ["string", "boolean", "object", "number", "array"],
      "description": "The value"
    },
    "isRequired": {
      "type": "boolean",
      "description": "Specifies whether the description is required or not."
    },
    "readOnly": {
      "type": "boolean",
      "description": "The value is maintained by the application and cannot be edited by an operator."
    },
    "secret": {
      "type": "boolean",
      "description": "The value is a secret (token/password/key): it is kept in the database only and is never shown in the UI or API output."
    },
    "restartRequired": {
      "type": "boolean",
      "description": "Changing the value takes effect only after the application is restarted."
    },
    "jsonSchema": {
      "$ref": "http://json-schema.org/draft-07/schema#"
    }
  },
  "required": ["key", "type", "name"],
  "additionalProperties": false,
  "if": {
    "properties": {
      "type": { "const": "json" }
    }
  },
  "then": {
    "required": ["jsonSchema"]
  }
}
